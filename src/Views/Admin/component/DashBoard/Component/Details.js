import React from 'react';
import PropTypes from 'prop-types';

Details.propTypes = {
    
};

function Details(props) {
    return (
        <div>
            
        </div>
    );
}

export default Details;